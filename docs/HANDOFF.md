# AKINTI — Devir Notu (Handoff)

Son güncelleme: 2026-09-03, gün sonu. Yarın buradan devam edilecek.

## Çalışma kuralları (kullanıcının talimatı)
- Ana oturum (Fable) **sadece yönetici**. Kod yazmaz. Tüm kod alt ajanlarla (Sonnet/Opus) yazılır.
- Opus sık sık 529 Overloaded veriyor; düşerse aynı brief ile Sonnet'e devret ("diskteki kısmi işi devral").
- Her ajana net dosya sahipliği ver; `git add <kendi yolları>` ile commit ettir; `.env.local` asla commit edilmez.
- Her aşamadan sonra ayrı bir doğrulayıcı ajan: typecheck, lint, test, build + güvenlik senaryoları.

## Ürün kararları
- Spec: `akın icin md.md` (tek kaynak). Marka **AKINTI**, nesne terimi **Wave**, **Beğeni yok**, arayüz **İngilizce**.
- Stack: Next.js 16.3 App Router, React 19, TS, Tailwind 4, Supabase (Postgres/Auth/Storage/Realtime), Postgres tabanlı iş kuyruğu + `scripts/worker.ts` (ffmpeg).
- Supabase anahtarları yok; `.env.local` boş. Kod `isSupabaseConfigured()` false iken dürüst "Backend not configured" gösteriyor. Uçtan uca test için kullanıcıdan URL + anon key + service role key istenecek.

## Repo
`c:\Users\alppr\akın proje\app` — git (yerel, remote yok), branch master, 24 commit.

## Tamamlanan aşamalar (commit'li)
- Stage 0-1: iskelet, dokümanlar (`docs/*`), 14+ migration + RLS, UI kit, app shell, global playback store, Waveform, WaveCard.
- Stage 2: auth + onboarding (route koruma `src/proxy.ts`).
- Stage 3: profiller, takip, engelleme, ayarlar (account/privacy/appearance/safety/follow-requests).
- Stage 4-5: istemci kayıt/yükleme/EQ önizleme; sunucu yükleme hattı, magic-byte doğrulama, yayınlama, Wave detay, `/api/audio/[assetId]/url`, Play/Replay tracker; audio_assets kolon güvenlik düzeltmesi; realtime publication migration; can_view_profile blocker düzeltmesi.
- Stage 10: mesajlaşma (metin, sesli mesaj, Wave paylaşımı, realtime, engelleme).
- Stage 11: bildirimler (gruplu liste, okunmamış rozeti, realtime + polling).

## Gün sonu durumu (üç ajan aceleyle bitirdi)
`git status` ile başla.
- **feed** (Stage 6-7): commit `45b448f` ile TAMAM. Home, Explore (7 kategori, rising creators, `rising_creators()` migration), `/search`. Eksik: `/`, `/explore`, `/search?q=a` curl ile canlı doğrulanmadı. Not: bu commit yarış yüzünden etkileşim ajanının bazı dosyalarını da (`src/components/comments/*`, `src/lib/interactions/*`, `w/[id]/interactions.ts`) içine aldı.
- **duet** (Stage 9): commit `7500041` (tek wip commit). Yapıldı: `src/lib/duet/{permissions,ffmpegChain,sync}.ts` + 42 test, worker (gelişmiş EQ uygulanıyor, adelay işaret hatası düzeltildi, `--dry-run`), `db/duets.ts`, 4 Server Action (requestDuet, respondToDuetRequest, cancelDuetRequest, publishDuetWave), 3 sayfa, 4 bileşen; typecheck temiz. EKSİK: lint/test/build tam çalıştırılmadı, `e2e/duet.spec.ts` yazılmadı, `docs/DUET_SPEC.md` güncellenmedi, DuetRecorder canlı test edilmedi. Bilinen sorun: `finalizeUpload`'un otomatik `process_audio` işi ile sonraki `mix_duet` işi aynı asset satırında yarışabilir (`create/duetActions.ts` başlığında not var) → ses hattı sahibine düzelttir.
- **interactions** (Stage 8): TAMAM. Commit `14d40b2` (+ `45b448f` içine karışan yorum dosyaları). Yorumlar, kaydetme (optimistic + rollback), paylaşım penceresi (link, native share, sohbete gönder), Request-a-Duet linki, `/settings/content/{saved,commented,waves,duets}`. 41 test. Açık nokta: DB seviyesinde yorum rate-limit yok (spec §39) → `comments_guard_insert` trigger migration'ı gerekiyor.

Yarın ilk iş: `git status` temiz olmalı; `npm run build` tam çalıştırılmadı (Duet ajanının dosyaları yazılırken denenmişti). Doğrulayıcı ajan ile başla.

## 2. gün ilerlemesi (2026-09-04)
- Doğrulama turu: typecheck/lint/test(386)/build temiz; SQL, güvenlik (§46, 10 senaryo PASS) ve entegrasyon incelemeleri yapıldı. `blocks.ts` sadeleştirildi (`dbbdb1a`).
- Duet TAMAM: `80528db`, `8fda07a` (duet stem'leri için çift işleme yarışı düzeltildi: `finalizeUpload(assetId, advancedEq?, skipAutoProcessing?)`), `2ba89ff` (docs + `e2e/duet.spec.ts`, E2E_SUPABASE ile gate'li). 423 test.
- Stage 12 (safety ajanı) çalışıyordu: rate limit trigger'ları, notification_preferences, moderasyon kuyruğu (/moderation), askıya alma, ayarlar > ses; ek migration'lar: audio_assets'i realtime yayınından çıkar (YÜKSEK bulgu), rising_creators() security definer, comment_permission tip daraltma.

- Stage 12 TAMAM (`d337914`, `82418ea`, `892bdc3`): rate limit trigger'ları (SQLSTATE AKRTL), notification_preferences, moderasyon (`/moderation`, resolve/dismiss, hide_wave, suspend), `/suspended`, ayarlar > ses; 3 ek düzeltme migration'ı.
- Stage 16 TAMAM (`7b890ee`, `68c5fb8`): CI, güvenlik başlıkları, env doğrulama, Dockerfile.worker, docs/DEPLOYMENT.md, docs/OPERATIONS.md.
- Stage 13 TAMAM (`16fa91c`, `7f0f24f`): creator RPC'leri, `/analytics`, `/analytics/health`, şüpheli oynatma işaretleme. Ayrıca `/api/*` public route düzeltmesi (anonim oynatma kırıktı).
- Stage 14 TAMAM (`e6c3ff7`, `9a53e1c`, `3d25a66`): askıya alma tüm Server Action'larda, rate-limit hata eşlemesi her yerde, profil sayfasında eager URL imzalama kaldırıldı, RecorderPanel lazy, wavesurfer bağımlılığı silindi. 470 test.
- Polish TAMAM (`348a9d4`, `a183877`): ölü kod silindi, (timestamp,id) composite cursor + `20260904090000_composite_pagination_indexes.sql`, 481 test, build temiz, tree temiz.
- Kalan küçük teknik borç: `duetRequests.ts`/`shares.ts`/`follows.ts`/`reports.ts`/`moderation.ts` hâlâ tek kolonlu cursor; knip'in 148 "kullanılmayan export" bulgusu (false positive'ler var, tek tek doğrulanmalı).

## Canlı arka uç (2026-09-04 öğleden sonra)
- Supabase projesi bağlandı: ref `apxdfjfrbrgdykycnoem`, bölge eu-west-1, DB bağlantısı `aws-1-eu-west-1.pooler.supabase.com` (session pooler; direct host IPv6-only olduğu için çalışmıyor). Tüm değerler `app/.env.local` içinde (`DATABASE_URL` dahil). **service_role anahtarı ve DB şifresi sohbete yapıştırıldı → yayına çıkmadan önce ikisini de Supabase panelinden yenile.**
- Araçlar: `npm run db:migrate` (tracking tablosu `schema_migrations`), `npm run verify:live -- --create-buckets`, `npm run worker` (ffmpeg 9 kurulu).
- 27 migration canlıda uygulandı (25 + e2e sırasında bulunan 2 RLS/constraint düzeltmesi). Bucket'lar oluşturuldu.
- Stage 15 TAMAM: `E2E_SUPABASE=1 npm run e2e` → 9/10 geçiyor; tek başarısız test Supabase'in e-posta gönderim kotası (`over_email_send_rate_limit`), kod hatası değil. E2E kullanıcıları admin API ile `email_confirm: true` oluşturuluyor (`e2e/helpers/supabaseAdmin.ts`). Bulunan 8 gerçek hata düzeltildi (giriş sonrası yönlendirme, onboarding yönlendirme, recorder snapshot, mime doğrulama, Play butonu URL çözümleme, RLS insert-returning, hesap silme constraint'i, worker env yükleme). Commit'ler `03d6879`…`721a37f`. 51 commit, 481 birim testi.
- Çalışan süreçler (oturum kapanınca ölür): dev server :3333, worker (`npm run worker`).

## TOKEN DİSİPLİNİ (2026-09-05 akşam, kullanıcı token harcamasından şikâyet etti; kabul edildi)
- Aynı anda en fazla 2 ajan, hepsi Sonnet. Opus yalnızca dalga sonu incelemesi için tek seferlik.
- Ajan raporları ≤20 satır (ne değişti + hash, kanıt, kalan). Uzun detay dosyaya (`docs/qa/<wave>/`) yazılır, sohbete gelmez.
- Compact sonrası Fable büyük dokümanları (PRODUCT_V2, DESIGN, research) YENİDEN OKUMAZ; sadece bu HANDOFF'un en üst bölümünü okur. Ajanlar CLAUDE.md'yi kendileri okur.
- Kullanıcıya önerilen ama onun yapacağı: claude-mem ve superpowers hook'larını bu projede kapatmak; ana oturumu Opus'a alıp Fable'ı yalnızca inceleme ajanı olarak çağırmak.

## DEVAM EDİLDİ (2026-09-05 akşam) — ajanlar yeniden başlatıldı
- Kontrol: portlar temiz; çalışma ağacında waveB dosyaları (create/audio/lib/audio) ve fixA dosyaları (Sheet, WaveCard, RecordKey, BottomNav, next.config, docs/qa/reviewA, kök `_qa-baseline*.mjs`) commit'siz. waveC'nin Wave sayfası commit'li (`93efb65`). Challenges hiç başlamamış.
- TAMAM: **fixA2** (`a927f1e`; Sheet autoFocus, WaveCard fullBleed, radius token'ları, devIndicators kapalı; 624 test, 18/18 Playwright, `docs/qa/fixA/`). Açık kalan: WavePlayer sol 72px inset (waveB2'ye iletildi); Escape sonrası odak body'ye dönüyor (vaul; A.2 polish'te).
- TAMAM: **challenges2** (`7bd64cf`; migration `20260905130000_challenges` canlıda; challenges/entries/picks + RLS + `enter_challenge`, `list_waves_by_hashtag` (waves.tags kullanır), actions, minimal sayfalar, seed 2 challenge, `scripts/verify-live-challenges.ts` 10/10; 634 test). Açık: `npm run seed:challenges` script'i yok (`npx tsx scripts/seed-challenges.ts`), SECURITY.md rate-limit tablosuna `challenge_entry` eklenmeli, create'e `?challenge=` parametresi lazım.
- TAMAM: **waveD** (`5495585`,`d00502c`,`58c6303`,`081f305`; DuetModePicker, AtismaTurnRecorder, DuetRecorder create bileşenleri üstünde, /challenges canlı-önce sıralama + backing track oynatıcı + mevcut Wave ile giriş + sayfalama, /hashtag/[tag]; build+634 test yeşil; `docs/qa/waveD/`). Zincir ağacı ve open call cevabı waveC2 tarafından `w/[id]/page.tsx` içinde yapılmış. Açık: mikrofonlu canlı duet QA yapılmadı → tam QA turunda.
- TAMAM: **polishA2** (`2ebdaf2`…`9ea684d`; PersistentPlayer (masaüstü+mobil mini şerit, tek audio), Sheet odak dönüşü + test, `/create?challenge=` + "Enter with a new Wave", RecordStage hideUpload/hideChooseTrack, `seed:challenges` script, SECURITY.md satırı; 641 test, build, axe temiz; `docs/qa/polishA2/`). BULDUĞU HATA: `rate_limit_events_action_known` CHECK'inde `challenge_entry` yok → challenge girişi DB'de düşüyor → challenges2 düzeltme migration'ı yazıyor (`20260905140000`).
- TAMAM: **e2efix** (`0a474d6`,`f4b2637`; auth/duet spec'leri flows.ts helper'larına geçti, private-Wave regex, duet spec 90 sn bütçe; e2e 9/10 (tek fail Supabase e-posta kotası); hesap silme: `deleteAccount` action + `DeleteAccountSheet`, admin deleteUser + cascade; 641 test).
- TAMAM: **challenges2 düzeltme** (`e6df999`; migration `20260905140000_rate_limit_challenge_entry` canlıda; verify-live-challenges 11/11, gerçek JWT ile enter_challenge regresyon kontrolü). Challenge girişi artık çalışıyor.
- TAMAM: **review2** (`059c634`; 7/10). P0: challenge_entries/picks select RLS `can_view_wave` yok (gizli Wave'ler anon'a açık). P1: hesap silmede storage nesneleri yetim; AtismaTurnRecorder iki ham `<audio>`; "Skip ahead" orijinali durdurmuyor; FALLBACK_PEAKS sahte düz dalga (DuetRecorder, CreateFlow). P2 (18): 8 eşzamanlı AudioContext (atismaAudio), segment aralıkları bildirilen süreden, imzalı URL yenilenmiyor, /hashtag ve /create param doğrulaması yok, publish progress'te Signal+sonsuz lamba, /create ve onboarding ortalanmış, em dash + "Supabase environment variables" kopyası, DSP zincirinde next/dynamic yok, 20 modül testsiz.
- TAMAM: **fixR2-backend** (`b6053c8` RLS P0 + migration `20260905150000` canlıda, verify 12/12; `c354eb3` hesap silme storage temizliği `src/lib/storage/userObjects.ts`; `e836040` imzalı URL yenileme `signedAudioUrl.ts` + playbackStore `assetId`; `cfc75b6` /hashtag ve /create param Zod; 668 test).
- TAMAM: **qa-full** (`d5e6688`; 6/10; `docs/qa/full/REPORT.md`). P0: mesajlaşma ilk DM'de 42P10 (get_or_create_direct_conversation ON CONFLICT partial index uyumsuz); ffmpeg ham stderr `processing_error` ile Wave sayfasına sızıyor. P1: Enhance'te RNNoise anahtarı yok; UploadDropzone input etiketi yok; `--akinti-ink-subtle` 4.22:1. P2: settings/account kart; upload yolu Enhance'i atlıyor; Lighthouse LCP 4.4–5.2 sn, JS 357–469 KB (bütçe 150). Geri kalan 25 ekran sıfır hata.
- TAMAM: **fixQA** (`8ab8455` DM upsert migration `20260905160000` canlıda + `scripts/verify-live-messaging.ts`; `f920398` sesli mesaj mime + finalizeMessageAudio processed_path; `9d68bf2` worker hata kodları `src/lib/worker/processingError.ts` + `getProcessingErrorMessage`, arnndn düşüşü; `03d4b1d` ink-subtle 4.55:1; `483c2bc` settings/account form; 751 test; `docs/qa/fixQA/`). Açık: ProcessingBanner.tsx tek satır → fixR2-frontend'e iletildi.
- TAMAM: **fixR2-frontend** (`047716c`…`7c3046e`, 12 commit; atışma store+pause, sahte peaks yok, tek AudioContext, publish progress ink, /create+onboarding sola yaslı, kopya temizliği, RNNoise anahtarı Enhance'te, dropzone etiketi, next/dynamic DSP, testler, ProcessingBanner mapper; 751 test; `docs/qa/fixR2/`). Not: playback `<audio>` DOM'a bağlı değil (querySelectorAll 0 döner, tasarım gereği).
- AÇIK (loose-ends ajanına): `DuetRequestsView.tsx` eski kart stili; `messages_payload_matches_kind` kısıtı cascade silmeyi engelliyor → mesajı olan kullanıcı hesap silemeyebilir (deleteAccount'u canlı test et, migration gerekebilir); test hesapları `e2e+fixr2*` yetim.
- TAMAM: **waveE-perf** (`c54e78a`,`8430453`,`01caed1`,`ab3da3f`; Sheet next/dynamic, immutable cache header'ları, `scripts/perf-budget.ts` + `npm run perf` (260 KB gzip/route bütçesi), `docs/qa/waveE-perf/ANALYSIS.md`). Sonuç: JS gzip Home 204 / Explore 204 / Wave 320 / Create 193 KB; Lighthouse 82/75/73/82; LCP hâlâ 4.5–5.4 sn → sebep ardışık Supabase sorguları (Suspense denendi, kötüleştirdi, geri alındı). 150 KB bütçesi bu stack'te ulaşılamaz (supabase-js ~85 KB).
- AÇIK → **perf2** (i18n'den önce, tek başına): Home/Explore/Wave veri çekimini paralelleştir veya tek RPC'ye indir (LCP hedefi <2.5 sn), Wave rotasını parçala (ShareSheet/WaveOwnerMenu/Composer next/dynamic), sonra Lighthouse'u aynı hesapla tekrar ölç.
- TAMAM: **waveE-pwa** (`1d5ed02`,`7303b5c`,`13f3ab4`,`90ecee6`; Serwist SW (classic type, `serwist build` adımı), manifest+ikonlar, `/~offline` (public allowlist'e eklendi), InstallHint + `markFirstPublish()`, push_subscriptions migration `20260905170000` canlıda, `src/lib/push/send.ts` `notifyDuetPush`, settings/notifications toggle; 751 test; `docs/qa/waveE-pwa/`). Açık: push toggle canlı doğrulanamadı (Playwright'ta Notification izni denied); entegrasyon satırları (CreateFlow markFirstPublish, Wave sayfasına InstallHint, duet actions'ta notifyDuetPush ×3) → loose-ends'e verildi. VAPID anahtarları `.env.local`'da (dev), prod için `.env.example`/DEPLOYMENT.md.
- TAMAM: **perf2** (`823a7b8` Promise.all, `4cf874c` React.cache session, `ad1c2a5` docs, `b570f92` düzeltme; 785 test). Sonuç: LCP değişmedi (4.6–5.3 sn); darboğaz DB değil, mobil simülasyonda kaynak yükleme (`docs/qa/perf2/LIGHTHOUSE.md`). Wave rotası next/dynamic denemeleri Turbopack'te bundle'ı büyüttü, geri alındı (322 KB, bütçe 260). Sayfa RPC'leri yapılmadı (gerekçeli). → **perf3** (sonra): kritik CSS/font stratejisi, ilk ekranın statik kabuğu, Wave rotası için gerçek parçalama; Lighthouse'u gerçek cihazda (WebPageTest/Moto G) doğrula.
- ÇALIŞIYOR: **perf3** (Sonnet, port 3610; LCP öğesi ve yükleme zinciri, font/critical CSS, hydration uzun görevleri, signed-out public kabuk (Explore, Wave) supabase-js'siz, avatar next/image, gerçekçi bütçe; `docs/qa/perf3/`).
- TAMAM: **waveF-frontend** (`5bbdb79` pitch_snap/self_harmony presetleri + sunucu kapısı + worker filtreleri; `fa84b3b` seed-plans; `7a7d5c9` Pro işareti; `d636c85` ProGate + useProStatus; `218c937` Pro ayar ekranı (iyzico sheet, Paddle overlay, @paddle/paddle-js); `b470d56` hydration düzeltmesi; `f17bce1` QA 21/21; 823 test; `docs/qa/waveF/`). Eksik → pitch-harmony'ye: Resume (sağlayıcı arayüzünde yok), enum migration (Pro presetler DB'de yok, şimdilik hep ProGate açıyor). `startProCheckout` ek `transactionId` alanı.
- TAMAM: **perf3** (`e2eb2de`; LCP öğesi metin (Wave başlığı), tek blokaj Tailwind CSS link'i → `experimental.inlineCss`; devtools throttling'de LCP Flow 2.3→1.4 sn, Explore 2.7→1.5 sn (hedefin altında); simulate modunda değişmedi (CPU çarpanı × hydration). Bütçe 340 KB. `docs/qa/perf3/`). Açık: `src/lib/auth/server.ts` `requireUser()` fazladan `suspended_until` sorgusu (TTFB) → i18n/temizlik turunda.
- TAMAM: **pitch-harmony** (`1e5f728` migration `20260906120000` canlıda (enum + pitch_score + `set_audio_asset_pitch_score`); `5a8839c` sidecar /pitch-score alanları + /pitch-snap + /harmony (pytest 11/11; akortlu 95.1, detone 43.0 → snap sonrası 93.3; harmony gerçek içerik ekliyor) + worker; `53b586f` PitchReport/OwnerInsights/PublishProgress; `9c60013` Pro kullanıcı gerçek seçim; `46d7597` resume (Paddle gerçek, iyzico dürüst red); `216ac49` test; 825 test). Eksik: kuyruk üstünden uçtan uca canlı çalıştırma ve `docs/qa/pitch/` ekran görüntüleri yok → final QA'da; CreateFlow publish sonrası hemen yönlendirdiği için PitchReport asıl Wave sayfası OwnerInsights'ta görülür.
- TAMAM (kısmi): **i18n** (`dbd8582` Flow layout dalı + requireUser fazla sorgu; `1a86c94` next-intl altyapısı (locale: profil → cookie → Accept-Language; migration `20260906130000_profile_locale` canlıda); `5e104cd` Flow/offline/manifest/auth çıkarımı; `f8ea629` `scripts/i18n-check.ts` ratchet + `docs/I18N.md`; `833a39e` kanıt; 834 test). KALAN: 137 dosya / 615 İngilizce metin baseline'da.
- TAMAM: **i18n-a** (`1e68c76` 60 dosya, `1736dc1` baseline yenileme, `32c40f0` kanıt; 56 namespace / 522 anahtar; baseline 137→8 dosya (b ile birlikte); `docs/qa/i18n-a/`). Kalan (i18n-c'ye): `ENHANCEMENT_PRESETS` ad/açıklamaları, `CREATION_TYPES` etiketleri, duet/duets/interactions actions hata metinleri, push alıcı-locale çözümü; Duet mod seçici TR ekran görüntüsü. Not: `ProfileHeader.test.tsx`/`Sheet.test.tsx` 10 test i18n-b çalışırken düşüyordu → b bitince kontrol.
- TAMAM: **i18n-b** (13 commit; 75 dosya; layout/profil/mesajlar/bildirimler/ayarlar/Pro/challenges/analytics/hashtag/tracks/moderation/pwa/Sheet; `navItems.ts`/`proIncludes.ts` anahtar tabanlı; next-intl vitest mock'ları; 834 test; `docs/qa/i18n-b/{tr,en}/`). Kalan → i18n-c.
- TAMAM: **i18n-c** (6 commit; 20 actions dosyası + moderation + Zod (`src/lib/validation/translate.ts`), push alıcı-locale, config sabitleri, Home/Settings/analytics kalanlar, i18n-check sunucu taraması 0; 889 test; `docs/qa/i18n-c/` TR hata metinleri canlı). Kalan: `mapAuthError` (`src/lib/auth/errors.ts`) İngilizce; 8 dosya/17 instance .tsx ratchet'te. Lint: flow dosyalarında fixR3'ün ara düzenlemeleri → fixR3'e iletildi.
- (eski) **i18n-c** (port 3665; actions formError/message, moderation/validation mesajları, push alıcı-locale, config sabitleri (presetler, CREATION_TYPES, DUET_MODE), Home başlığı, settings/analytics kalanlar, i18n-check .ts taraması, I18N.md "complete").
- TAMAM: **review3** (`73cf04c`; 5/10, REQUEST CHANGES). P0 ×5: (1) subscriptions + flow migration'ları `rate_limit_events_action_known`'ı yeniden yazıp `billing_checkout`'u siliyor → her Pro checkout düşer; (2) CSP `script-src 'self'`/`frame-src 'none'` Paddle ve iyzico script/iframe'ini engelliyor; (3) `enqueue_audio_job` authenticated'a açık, payload doğrulanmıyor → `preset:'pitch_snap'` ile Pro kapısı atlanır; (4) `audio_assets_guard_update` `enhancement_preset`'i korumuyor → ikinci bypass; (5) playback `<audio>` `crossOrigin` yok → `createMediaElementSource` cross-origin imzalı URL'de sessiz → Flow izi ölü. P1 ×10 (hesap silme aboneliği iptal etmiyor, applyBillingEvent retry'da geçiş kaybı, iyzico bilinmeyen event→past_due, push endpoint SSRF, SW auth HTML cache, Flow seed sayfa uyumsuzluğu, 59 KB mesaj bundle'ı, count_flow_new full scan, /flow'da ham Postgres hatası, billing state machine testsiz). P2 ×21. review2: 13/16 kapalı.
- TAMAM: **fixR3** (P0: `85a5447` rate_limit_actions lookup tablosu (canlı), `1d0c1af` CSP Paddle/iyzico, `bcb252d`+`f3760db` enqueue_audio_job + preset guard (canlı negatif testler), `6498a98`+`332458b` crossOrigin + analyser (0.224 vs 0.0); P1: `95ac0e3` silmeden önce abonelik iptali, `3954625` idempotent applyBillingEvent + 15 test, `ea3620b` push allowlist, `4c1abdf` SW NetworkOnly, `76fabda` Flow seed cursor'da + ham hata yok, `0c26465` count_flow_new sınırlı + index, `85353f5` review2 kalanları, `486ff78` (auth) mesajları daraltıldı, `d4f4b62` lint; 889 test, tüm verify-live script'leri yeşil; `docs/qa/fixR3/`). Kalan: (app) grubu tam mesaj bundle'ı; perf-budget /w/[id] 344 vs 340 KB → fixP2.
- TAMAM: **qa-full2** (7/10, P0 yok; önceki P0/P1'ler canlıda kapalı; `docs/qa/full2/REPORT.md`, 69 ekran; rapor commit'i başka ajanın `068a647` commit'ine karıştı, içerik tam). P1 ×4: Settings/Appearance mor-gradient presetleri (inert, kartlı); Pro/TRY akışı plan kontrolünden önce TC kimlik istiyor, İngilizce hata; challenge içerikleri İngilizce + em dash (seed); TBT Flow 2100 / Explore 1330 ms. P2 ×5: Flow aria-hidden-focus, perf-budget 344/340, publish formunda İngilizce tür etiketleri, React #418, 15+ dosyada kart. Dil bulguları 6. Lighthouse devtools: Flow 69 / LCP 2.3 s, Explore 70 / 2.6 s, Wave 86 / 1.5 s, Create 91 / 1.2 s.
- TAMAM (kısmi): **fixP2** (22 commit `3db8160`..`4e7983d`; review3 P2 19/21 + mapAuthError TR + i18n ratchet sıfır (baseline silindi, 1370 anahtar parite); 895 test). YAPILMADI: (app) mesaj daraltma, perf-budget 344/340, `docs/qa/pitch/` ekranları; İKİ MİGRATION CANLIYA UYGULANMADI (Flow davet dedup + `has_pro` anon revoke) → closeout ajanı `npm run db:migrate` çalıştırıp doğrulayacak; build/perf koşulmadı.
- TAMAM: **fixQA2** (`0ead4c4` Appearance → tema + imza tonu, migration `20260906220000` canlıda; `79a8025` Pro/TRY sıra + TR hatalar; `60e2e67` challenge `title_tr/brief_tr` migration `20260906220100` canlıda + yeniden seed; `f9d03a4` ShareSheet ertelendi (TBT ölçümü bu hostta güvenilmez, `docs/qa/fixQA2/TBT.md`); `8428391` inert; `d4b499d` tür etiketleri TR; `570caee` 3 gerçek #418; `15073eb` 14 dosyadan kart kaldırıldı; `714974d` kanıt; 895 test).
- GITHUB (2026-09-06): özel repo https://github.com/alpbazoglu/akinti (`gh` ile alpbazoglu hesabı; `app/` klasörü repo kökü; `origin/master`). Gizli dosya taraması temiz (.env.local yok, geçmişte anahtar yok). Kullanıcı projeyi başka şehirdeki arkadaşına devredecek; arkadaş kendi PC'sinde devam edecek → **handover** ajanı `app/docs/HANDOVER.md` (TR: sıfırdan kurulum, env, Supabase üyelik, migration, worker/sidecar, yayın, anahtar rotasyonu, doküman haritası) yazacak; `.env.local` ayrı kanaldan gönderilecek; Supabase panelinden arkadaş projeye member eklenecek. Bu HANDOFF.md'nin kopyası `app/docs/HANDOFF.md` olarak repoya girer.
- TAMAM: **closeout** (10 yerel commit; 56 migration hepsi canlıda, 5 verify-live yeşil; gate'ler yeşil (895 test); Tailwind `@source not` docs/scripts (dev 500 düzeldi); `/w/[id]` 343→255 KB, diğer rotalar ~313→227 KB (zod sızıntısı `CommentComposer` sabiti); (app) mesaj daraltma nested provider'lar; `docs/qa/pitch/` gerçek pYIN ekranları; e2e duet 3/3, auth 3/4 (e-posta kotası), critical 2/3 (180 sn cap, yük); CI `branches: [master]` + `E2E_ENABLED` repo var; DEPLOYMENT/README/`seed:plans`).
- TAMAM: **handover** (`cfb1154` docs/qa/full2 (Lighthouse JSON'larındaki test hesabı oturum çerezleri redakte edildi → founder bu iki `e2e+f2*@akinti.test` hesabını Supabase'den silmeli); `7f05131` `docs/HANDOVER.md` ~780 satır TR + README). Doğrulanmadı: `supabase start` yerel yolu, telefon-LAN (`allowedDevOrigins`), akinti.app, sandbox ödeme.
## MASAÜSTÜ YENİDEN TASARIM (2026-09-06, kullanıcı: "masaüstü frontend çok kötü, sayfalar arası bekleme ikonu yok, tıklama hissi yok, renkler kötü, ikon yok; 21st.dev ve gerçek uygulamalardan araştır")
- TAMAM: **r-desktop** (`b54491a`; 16 ürün karşılaştırması, 21st.dev katalog (kota 2/gün), 6 repo, iki yön mock'u). KULLANICI SEÇİMİ: **A "Akış"** (koyu-öncelikli, katmanlı, teal `#2FE0C9` + amber `#F0A63F`, Signal canlı kayıt) → `docs/design/DESIGN_V3_DESKTOP.md` (`27deb62`; ≥1024px'te DESIGN/COLOR_V2'yi ezer, kart/gölge serbest, feedback kuralları).
- TAMAM: **r-feedback** (`821fca8`; sıfır loading.tsx, sıfır useLinkStatus; tıklama sonrası 689–1160 ms hiçbir değişiklik; SideNav aktif durumu usePathname'e bağlı; Button primary/secondary hover yok, cursor default; main 600px sabit, ekranın %44–58'i boş; 6 rota nav'da yok). Bulgular desktop-shell'e zorunlu kabul kriteri olarak iletildi.
- KESİLDİ (oturum kapandı): **desktop-shell** → `49cdf66` token'lar v3 commit'li; kabuk dosyaları (DesktopSideNav, DesktopTopBar, NowPlayingBar, CommandPalette, RouteProgress + AppShell/SideNav/navItems/icons/messages değişiklikleri) diskte commit'siz kaldı. Ayrıca `99a36cd` araştırma .tsx kopyaları .txt yapıldı (tsc engeli).
- TAMAM: **desktop-shell2** (`56d656e` kabuk v3: sidebar 240/72, top bar, NowPlayingBar, CommandPalette, RouteProgress; `17503af` 17 loading.tsx + Button/IconButton cursor+hover + `useActionToast`; `51faac6` DNA; `c0a18cb` a11y + kanıt `docs/qa/desktop-shell/`). Ölçüm: tıklama→görünür tepki 30 ms (önce 700–1200), main 600→1136px, 6 rota nav'da, axe 0, 895 test, perf bütçe içinde. Kalan → desktop-screens: useOptimistic/useActionToast çağrı yerleri, iskelet şekilleri.
- TAMAM (kısmi): **desktop-screens** (`bd92daf` Flow stage+rail, `a65f674` viewport hook, `87f3cf5` Explore grid, `0ec813a` Wave iki kolon, `163ac2a` Profil, `de99da6` çağrı yerleri toast+guard, `53e79fe` kanıt, `1f2a6e2` iskeletler + SCREENS.md Desktop v3; 895 test; `docs/qa/desktop-screens/`). Not: ≥1024px koyu varsayılan globals.css'te. Kalan: Create iki kolon, Challenges/Tracks/Duets/Search/Notifications/Messages/Settings/Analytics.
- TAMAM: **desktop-screens2** (`ea60f01` Create iki kolon + `useStageShortcuts`; `aba53a6` Messages split-pane; `700d9a9` Settings nav rail + Pro kart seçici + Appearance önizleme; `f982819` Notifications sticky header + inline ses; `44bd9ab` iskeletler + SCREENS.md; `62105d8` kanıt `docs/qa/desktop-screens2/`; 895 test, 0 hata/axe).
- TAMAM: **desktop-screens3** (`bfc19a3` Challenges/hashtag, `d4f5505` Analytics dataviz, `fc46e99` Tracks, `f47901c` Search, `437ac3a` Duets inbox + mod kartları, `e7da716` kanıt `docs/qa/desktop-screens3/`, `1967297` axe fix; 895 test). Kalan → desktop-duet.
- TAMAM: **qa-desktop** (`2b47054`; 7.5/10, P0 yok; tıklama→tepki 689–1160 → 88–118 ms; loading.tsx 0→17; cursor pointer; main akışkan; 4 şikâyet de "düzeldi"). P1: yeni Wave'e başka hesap Duet isteği atamıyor (izin seçimi gizli/varsayılan yanlış). P2: sidebar "New Wave" hover yok; Pro plan kartları ödeme kurulu değilken boş; RouteProgress probe yakalayamadı; canlı Explore test hesabı çöplüğü. `docs/qa/desktop/REPORT.md`, 69 ekran.
- TAMAM: **fixDesktop** (`806d7fa` Duet izni kökten: CreateFlow openCall'ı hiç bağlamamış, select boş; varsayılan "everyone" + migration `20260907090000_wave_duet_default` canlıda, iki hesapla UI repro; `ce5339d` New Wave hover; `43defe7` RouteProgress probe 7–14 ms; `12f19a8` `cleanup:test-accounts` (5 hesap silindi); Pro boş durum kopyası `82a0103` içinde; `a81d67f` kanıt; 896 test).
- MASAÜSTÜ YENİDEN TASARIM TAMAMLANDI (2026-09-07). QA 7.5/10 → P1 kapandı. Push: kullanıcı "tamamen bitince" dedi → bu noktada push yapıldı (41 commit).
- (eski) **fixDesktop** (port 3832; duet izni kök düzeltme + migration `20260907090000` gerekirse, New Wave hover, RouteProgress probe, `cleanup:test-accounts` script'i, Pro boş durum kopyası).
- TAMAM: **desktop-duet** (`82a0103`,`cabb29a`,`9746b1f`,`aed2092`; Atışma/Cypher iki kolon stage + turn listesi + dört hue verse sırası, `DuetChainTree` (Wave sayfası + /duets detay), /w/[id]/duet iskeleti; 896 test; `docs/qa/desktop-duet/`). Eksik: /w/[id]/duet/record iskeleti. Not: eşzamanlı build'ler `.next`'i eziyor → QA ajanları `next start` için ayrı `distDir` kullanmalı (sonraki brief'lere ekle).
- (eski) **desktop-duet** (port 3821; Atışma/Cypher iki kolon stage, `DuetChainTree` bileşeni, /w/[id]/duet iskeleti) ve **qa-desktop** (port 3810; bağımsız masaüstü QA → `docs/qa/desktop/REPORT.md`; feedback ölçümleri önce/sonra, mock-A sadakati, şikâyet bazında karar).
- SONRA: QA bulguları düzeltme (fixDesktop) → kullanıcıya ekranlar → onayla push.
- (eski) **desktop-screens3** (port 3799; Challenges/hashtag, Tracks, Duets, Search, Analytics).
- (eski) **desktop-screens2** (port 3788; Create/Enhance/Publish iki kolon + kısayollar, Messages iki pane, Settings nav+form, Notifications; iskeletler) ve **desktop-screens3** (port 3799; Challenges/hashtag, Tracks, Duets/mod seçici/Atışma-Cypher stage, Search sekmeleri, Analytics dataviz; iskeletler). Messages dosyaları paylaşımlı.
- SONRA: masaüstü QA (qa-desktop) → kullanıcıya ekranlar → onayla push.
- (eski) **desktop-screens** (designer, port 3777; Flow sağ ray, Explore kart grid, Wave iki kolon, Profil banner+grid, Create iki kolon, diğer ekranlar, çağrı yerlerinde optimistic+toast, iskelet şekilleri; `docs/qa/desktop-screens/`).
- (eski) **desktop-shell2** (designer, port 3766; diskteki kısmi işi devralıp bitirir; kabul kriterleri FEEDBACK_AUDIT sayıları).
- (eski) **desktop-shell** (designer, port 3755; token'lar v3, sidebar 240/72 + now-playing bar 88px + top bar + ⌘K paleti, route progress + loading.tsx her rota + useLinkStatus + Button :active/hover/focus + toasts + optimistic, ikonlar, yoğunluk; `docs/qa/desktop-shell/`). SONRA: **desktop-screens** (ekran içleri: Flow sağ ray, Explore kart grid, Wave, Create, Profile grid, Challenges/Tracks kartları) → QA.
- (eski) **r-desktop** (designer; Spotify/SoundCloud/TikTok/Linear vb. masaüstü referansları, 21st.dev bileşenleri (MCP), GitHub repoları, frontend skill; çıktı `docs/research/desktop/DESKTOP_RESEARCH.md` + iki yön mock'u `mock-A.png` (koyu, zengin) / `mock-B.png` (açık, editoryal)) ve **r-feedback** (port 3744; rota başına yükleme/press/hover/ikon/yoğunluk ölçümü → `FEEDBACK_AUDIT.md`).
- SONRA: kullanıcıya iki mock'u göster, seçtiği yönle DESIGN_V3 kuralı (kart/gölge yasağı gevşer, koyu-öncelikli olabilir) → **desktop-shell** (sidebar+ikon, now-playing bar, progress bar, loading.tsx her rotaya, useLinkStatus, Button :active, hover, optimistic follow/save, toasts) → ekran ekran masaüstü → QA.
- V2 PROGRAMI TAMAMLANDI (2026-09-06). Son push ile arkadaşa devir. Kullanıcı bir Bluehost satın alma sordu: ajan kayıtlarında bluehost yok, dış tarayıcı gezintisi yok; bizden değil (yenileme ya da phishing olabilir).
- SONRAKİ (arkadaş / gelecek oturum): HANDOVER.md §(j) açık işler; anahtar rotasyonu; Vercel + VPS; gerçek cihazda Flow dinleme; sandbox ödeme.
- PUSH KURALI: kullanıcı "tamamen bitince push" dedi → handover bitince Fable tek push atar, arkadaşa link o zaman verilir.
- (eski) **closeout** (port 3733; bekleyen migration'lar + tüm verify-live, tüm gate'ler, perf-budget /w/[id], (app) mesaj daraltma, `docs/qa/pitch/`, e2e, DEPLOYMENT.md prod checklist, README).
- (eski) **fixQA2** (Sonnet, port 3722; Appearance → tema + imza tonu (migration `20260906160000`), Pro/TRY sıra + TR hatalar, challenge iki dilli içerik (migration `20260906170000`), TBT profili, aria-hidden, tür etiketleri, #418, kartlar; `docs/qa/fixQA2/`).
- (eski) **qa-full2** (Sonnet qa-tester, port 3700 + worker + sidecar; TR+EN, Flow, Pro, pitch, PWA, iki tema, Lighthouse devtools → `docs/qa/full2/REPORT.md`).
- ÇALIŞIYOR: **fixP2** (Sonnet, port 3711; review3 P2 ×21, mapAuthError TR, .tsx ratchet sıfırlama, (app) mesaj daraltma, perf-budget 4 KB, `docs/qa/pitch/` ekranları).
- (eski) **fixR3** (Sonnet, port 3676; tüm P0 ve P1'ler + review2 kalanları; migration'lar `20260906140000` (rate limit action birliği, regresyon önleyici yapı) ve `20260906150000` (enqueue guard + preset guard); CSP; crossOrigin + analyser guard; billing idempotency/testler; push allowlist; SW nav network-only; mesaj bölme; `docs/qa/fixR3/`).
- (eski) **review3** (Opus, salt okunur; `059c634..HEAD` güvenlik+doğruluk+lansman hazırlığı → `docs/qa/review3/REVIEW.md`).
- (eski) **i18n-b** (port 3654; flow, explore/feed, create/audio, duet, w/duets/wave/comments/share, onboarding, auth) ve **i18n-b** (port 3654; home/profile/messages/notifications/settings/analytics/challenges/hashtag/tracks, ui/layout/pro/pwa, push, moderation/validation mesajları). Messages dosyaları paylaşımlı: sadece kendi namespace'leri.
- (eski) **i18n** (Sonnet, port 3632; next-intl prefix'siz, cookie/profil locale, `src/messages/{tr,en}.json`, tüm string'ler, `scripts/i18n-check.ts`, Settings dil satırı, `docs/I18N.md`; ayrıca Flow overlay → layout dalı, `requireUser()` fazla sorgu; `docs/qa/i18n/`).
- (eski) **pitch-harmony** (Sonnet, port 3621; enum + pitch_score migration `20260906120000`, sidecar /pitch-score alanları + /pitch-snap + /harmony, worker entegrasyonu, PitchReport UI (publish done + Wave sayfası OwnerInsights), Pro kullanıcı için gerçek seçim, billing resume; `docs/qa/pitch/`).
- (eski) **waveF-frontend** (Sonnet, port 3599; Pro ayar ekranı (durumlar, TRY/USD, Paddle overlay, "ödeme kurulu değil" durumu), Pro işareti profil+WaveCard, Enhance'te 2 Pro ses + ProGate sheet, sunucu tarafı `requirePro` kapısı, `scripts/seed-plans.ts`; `docs/qa/waveF/`).
- TAMAM: **flow** (`3640662` migration `20260906110000_flow` canlıda + `src/lib/db/flow.ts`; `c92d6bc` /flow ekranı + analyser + actions; `3ae57c3` varsayılan rota + nav ilk sıra; `b45f1d9` swipe dvh düzeltmesi + gerçek giriş sonrası yönlendirme; `1f2a4e1` FLOW.md notları; 823 test; 19/20 Playwright, `docs/qa/flow/` ekran + 2 video). Eksik: tam ekran `fixed inset-0 z-40` overlay (AppShell değişmedi, kabuk DOM'da altında duruyor → i18n/perf3 turunda layout düzeyine taşınmalı); davet slotu (backing track/open call) canlı UI'da görülmedi; ArrowUp testi veri sınırında düştü.
- (eski) **flow** (Sonnet, port 3588; FLOW.md: migration `20260906110000_flow` (flow_impressions, get_flow_page, record_flow_event, count_flow_new), /flow ekranı, analyser'lı FlowTrace, swipe/auto-advance/prefetch, nav'da ilk sıra ve giriş sonrası varsayılan; `docs/qa/flow/`).
- (eski) **perf2** (Sonnet, port 3566; Home/Explore/Wave sorgu şelalesi → Promise.all + sayfa RPC'leri migration `20260906090000`, session `cache()`, Wave rotası next/dynamic; hedef LCP<2.5 sn; `docs/qa/perf2/`).
- TAMAM: **loose-ends** (`d1b7e8d` hesap silme cascade migration `20260905180000` canlıda + `scripts/verify-live-delete.ts` PASS + 9 yetim test hesabı silindi; `dd9042d` DuetRequestsView rail satırları; `caa39a6` markFirstPublish + notifyDuetPush ×3; `docs/qa/loose-ends/`). Açık: Wave sayfasındaki `<InstallHint />` perf2'nin yeniden yazımında düşmüş → perf2'ye iletildi.
- KULLANICI KARARLARI (2026-09-06): (1) "hep siyah beyaz" → renk yönü **Akıntı: su tonları** seçildi → `app/docs/design/COLOR_V2.md` (`82c2c0b`), CLAUDE.md renk kuralı güncellendi; (2) "göze hitap etsin, Reels gibi bağımlı yapsın" → **Flow** tam ekran kaydırmalı dinleme akışı → `app/docs/FLOW.md`. (3) iyzico: kullanıcı kaydolmadı; kod sandbox'a göre yazılıyor, anahtar yok. Kullanıcı için dev server 3333'te açık (arka plan).
- TAMAM: **colour** (`0f1537a`…`9cc02ca`, 7 commit; token'lar, waterline `hue` prop, RecordKey, ui/layout bileşenleri, `genreHue.ts`, /kit Hues; sand light `#8C6418`, foam dark `#3E7972` olarak düzeltildi; 785 test, axe temiz; `docs/qa/colour/` iki tema × iki viewport). Eksik: Atışma/Cypher segment ve zincir çizgisi renkleri render yüzeyi olmadığı için uygulanmadı (Wave detay/EnhanceStage başka ajanın); Explore tür tonu gerçek veriyle beslenmiyor; manifest splash renkleri v1'de.
- (eski) **colour** (Sonnet designer, port 3577; COLOR_V2 token'ları, waterline hue'ları, buton/tab/sheet, mod ve tür renkleri, /kit; `docs/qa/colour/`).
- SIRADA: closeout bitince (bekleyen migration'ları uygula + verify-live hepsi, build/perf yeşil, (app) mesaj daraltma, perf-budget, pitch ekranları, son typecheck/lint) → kullanıcıya rapor ve ekranlar → kullanıcı gerçek cihazda Flow/kayıt testi → yayın hazırlığı (anahtar rotasyonu, Vercel, worker/sidecar Docker, Auth redirect URL'leri, VAPID prod, iyzico/Paddle anahtarları) (Pro checkout'u sandbox anahtarı olmadan "payments not set up" durumuna kadar; Flow'u gerçek cihazda dinleme kontrolü kullanıcıdan) (TR+EN, Flow, Pro, pitch, PWA dahil) → review3 + QA bulguları düzeltme turu (2 ajan) → yayın hazırlığı (anahtar rotasyonu, Vercel, worker/sidecar Docker, Auth redirect URL'leri, VAPID prod, iyzico/Paddle anahtarları) → düzeltme turu → yayın hazırlığı (anahtar rotasyonu, Vercel, worker/sidecar Docker, Auth redirect URL'leri).
- TAMAM: **waveF-backend** (`33278b3` şema migration `20260906100000_subscriptions` canlıda; `d9edb6d` iyzico (SDK, `serverExternalPackages`) + Paddle sağlayıcıları; `9a665c2` webhook/imza testleri; `c1d92cb` webhook + callback route'ları; `f8266b3` settings/pro actions; `125796e` `docs/BILLING.md` + `verify-live-billing.ts`; 785 test, build yeşil). Founder'dan gerekecek env: IYZICO_API_KEY/SECRET_KEY/BASE_URL, PADDLE_API_KEY/WEBHOOK_SECRET/ENVIRONMENT, NEXT_PUBLIC_PADDLE_CLIENT_TOKEN (bkz. `.env.example`). Süreç notu: iki kez başka ajanların `git add -A` ile dosyaları karıştı → tüm ajanlara açık path hatırlatması yapıldı.
- (eski) **waveF-backend** (Sonnet; Pro abonelik: migration `20260906100000_subscriptions`, iyzico + Paddle sağlayıcı katmanı `src/lib/billing/**`, webhook route'ları, settings/pro actions, `has_pro`/`requirePro`, `docs/BILLING.md`; UI sonraki ajanda). Founder'dan gerekecek: iyzico ve Paddle sandbox/prod anahtarları.
- (eski) **loose-ends** (Sonnet, port 3555; hesap silme cascade migration `20260905180000` + `verify-live-delete.ts` + yetim test hesapları temizliği, DuetRequestsView kart stili, CreateFlow `markFirstPublish()`).
- (eski) **waveE-perf** (Sonnet, port 3544; bundle analizi, fontlar, LCP, JS bütçesi, cache header'ları, perf bütçesi script'i; `docs/qa/waveE-perf/`).
- ÇALIŞIYOR: **waveE-pwa** (Sonnet, port 3533; @serwist/next SW + manifest + ikonlar, ilk publish sonrası InstallHint, push_subscriptions migration `20260905170000` + web-push + settings/notifications toggle, ~offline sayfası). Açık kalacak: `CreateFlow.tsx` içinde `markFirstPublish()` çağrısı.
- (eski) **fixQA** (Sonnet, port 3522; mesajlaşma migration `20260905160000`, worker hata kodu/sanitize + arnndn fallback, ink-subtle kontrast, settings/account form; sahiplik migrations, worker.ts, terminology/format, globals.css, settings/**).
- ÇALIŞIYOR: **fixR2-frontend** (Sonnet, port 3511; +QA'dan RNNoise anahtarı, dropzone etiketi, upload→Enhance kararı; review2 UI/audio bulguları: AtismaTurnRecorder store + pause, FALLBACK_PEAKS, AudioContext, decoded süre, publish progress, ortalanmış layout'lar, kopya, next/dynamic, testler; `docs/qa/fixR2/`).
- (eski) **fixR2-backend** (Sonnet; P0 RLS migration `20260905150000`, hesap silme storage temizliği, imzalı URL yenileme, param doğrulama; sahiplik migrations, lib/db, settings/actions, lib/audio/playbackStore + URL resolver, hashtag, create/page.tsx param).
- SIRADA: perf2 bitince **waveE-i18n** (tek başına; waveF-backend UI'a dokunmadığı için paralel olabilir) → **waveF-frontend** (Pro ekranı, paywall, Pro rozeti, pitch skoru, harmony) → final QA + review3 (TR first-class; tüm ekranlara dokunur, tek başına koşmalı) → kısa doğrulama turu → Wave F (iyzico+Paddle Pro, pitch skoru, harmony) → Wave F (iyzico+Paddle Pro, pitch skoru, harmony). Lansman öncesi: service_role + DB şifresi rotasyonu, Vercel deploy, worker/sidecar Docker, Supabase Auth redirect URL'leri.
- ÇALIŞIYOR: **qa-full** (Sonnet qa-tester, port 3500; bağımsız tam QA → `docs/qa/full/REPORT.md`; kod düzeltmez, sadece raporlar).
- (eski) **polishA2** (Sonnet, port 3499; masaüstü kalıcı oynatıcı/mobil mini şerit + Media Session, Sheet odak dönüşü, create `?challenge=`, RecordStage duet props, seed script, SECURITY.md satırı; `docs/qa/polishA2/`).
- (eski) **waveD** (Sonnet, port 3488; duet istek/open call/mod seçici, layer/atışma/cypher kayıt ekranları (create bileşenlerini import eder), zincir ağacı, /challenges ve /tags ekranları; `docs/qa/waveD/`).
- (eski) **challenges2** (Sonnet; challenges/entries/picks + RPC + actions + seed; sahiplik: migrations challenges*, lib/db/challenges.ts, (app)/challenges/**, scripts/seed-challenges.ts, docs/CHALLENGES.md).
- TAMAM: **waveB2** (`2031bb2`…`6d52574`; CreateFlow gerçek adım makinesi: kayıt→sayım→trim→enhance→detay→publish, yükleme yolu, /tracks + `?track=` ile altyapı üzerine kayıt, WavePlayer full-bleed düzeltmesi, lint temiz, 624+ test, Playwright fake mic 390×844/1280×800 sıfır hata; `docs/qa/waveB/`). Açık: `e2e/helpers/flows.ts` etiket uyumsuzluğu → waveC2'ye verildi.
- TAMAM: **waveC2** (`b0e4155`…`1c9e7ab`; onboarding 3 adım `src/app/onboarding/**`, profil imza izi, ayarlar hub, analitik, mesajlar, bildirimler, flows.ts düzeltmesi; 636 test, build yeşil; `docs/qa/waveC/`). Açık: onboarding 10 sn sesli tanıtım (profiles kolonu yok, migration lazım), ayarlarda hesap silme, WavePerformanceTable sparkline yok (peaks verisi yok), `e2e/auth.spec.ts` + `e2e/duet.spec.ts` eski onboarding akışı → e2efix'e verildi.
- ÇALIŞIYOR: **e2efix** (Sonnet; e2e spec'leri yeni onboarding'e uydur, private-Wave regex, duet worker timeout, ayarlarda hesap silme; sahiplik e2e/**, settings/**).
- (eski) **waveC2** (Sonnet, port 3477; kalan ekranlar: Home, Onboarding, Profile, Messages, Notifications, Settings, Analytics, auth, Wave sayfası owner menüsü/processing banner; `docs/qa/waveC/`).
- (eski) **waveB2** (Sonnet, port 3455; sahiplik: create/**, components/audio/**, lib/audio/**, (app)/create/**, /tracks, package.json) ve **fixA2** (Sonnet, port 3466; sahiplik: Sheet, WaveCard, RecordKey, BottomNav, TopBar/SideNav, next.config devIndicators, docs/qa/reviewA, kök _qa-baseline dosyaları).
- SIRADA: qa-full raporu → P0/P1 düzeltme ajanları (2'şer) → tek seferlik Opus kod incelemesi (güvenlik dahil); sonra E (PWA/perf/TR) ve F (Pro/pitch/harmony) → bağımsız tam QA → E → F (SCREENS.md'ye göre kalan ekranlar: Home, Profile, Messages, Notifications, Settings, Analytics, onboarding; owner menüsü + processing banner Wave sayfasında var mı kontrol). Sonra Wave D ön yüzü, A.2 polish, tam QA, E, F.

## (önceki) DURDURULDU (2026-09-05 akşam) — kullanıcı "durdur her şeyi" dedi; tüm ajanlar öldürüldü
- TAMAM ve commit'li: waveA (8af1a9a+), fix-audit, wb-sidecar, wd-backend (`4851b78`…`9b5afe8`: open calls, duet_tree, atisma/cypher, worker; 5 migration canlıda), reviewA raporu (`docs/qa/reviewA/REVIEW.md`, 8/10).
- YARIDA KESİLDİ (çalışma ağacında commit'siz dosyalar olabilir; devam edince önce `git status --short`):
  - **waveB** (Record/Enhance/Publish ön yüzü): `src/components/create/**` (RecordStage.tsx vb.), `src/components/audio/**`, `src/lib/audio/**`, `src/app/(app)/create/**`, `/tracks`; en son upload yolunu yeniden yazıyordu.
  - **waveC** (tüm ekranlar): `src/app/(app)/**` sayfaları, `src/components/{feed,profile,messages,notifications,comments,share,analytics,moderation}/**`, `(auth)/**`; en son Wave sayfası owner menüsü + processing banner'daydı.
  - **fixA** (reviewA P0/P1): Sheet.tsx odak tuzağı, WaveCard stream full-bleed, tek auth CTA, dev indicator, radius token'ları; doğrulama aşamasındaydı.
  - **challenges-backend**: challenges/entries/picks şeması + RPC + actions + seed; henüz başlangıçtaydı (migration yazılmamış olabilir).
- DEVAM EDİNCE: her biri için "diskteki kısmi işi devral, tamamla, kendi dosyalarını commit et" brief'iyle aynı isimde yeni ajan (waveB2, waveC2, fixA2, challenges2); dosya sahipliği yukarıdaki gibi. Sonra: Wave D ön yüzü (duet ekranları; waveB'nin kaydedicisini kullanır), A.2 polish (masaüstü kalıcı oynatıcı, profil imza izi), bağımsız tam QA, Wave E (PWA/perf/TR), Wave F (iyzico+Paddle Pro, pitch skoru, harmony).
- Süreçler: agentların açtığı dev server'lar (3444/3555/3666/3777/3888) ve worker/sidecar süreçleri kapatılmaya çalışıldı; devam edince port çakışması olursa `Get-NetTCPConnection` ile temizle.

## (önceki) GÜNCEL (2026-09-05 öğleden sonra) — AŞAMA 0 TAMAM, AŞAMA 1 TAMAM, AŞAMA 2 BAŞLADI
- Aşama 0 çıktıları commit'li (`eb0ac64`): `app/docs/research/{2026-09-market-research,mobile-guidelines,libraries}.md`, `teardown/TEARDOWN.md`, `ux-audit/REPORT.md` (5.5/10), `app/docs/design/{DESIGN.md,DESIGN_DNA.json,SCREENS.md,REFERENCES.md}`.
- Aşama 1 TAMAM: `app/docs/PRODUCT_V2.md` (öncelikler, dalga planı A–F) ve `app/CLAUDE.md` (kural kitabı: tasarım, mobil, mühendislik, doğrulama, çoklu ajan disiplini). Karar: streak/rozet P0'dan çıktı (DESIGN §12/35).
- Tasarım özeti: "waterline" tek çizim ilkesi; Archivo Variable + Martian Mono; Signal `#DE3C11`/`#FF5C33` sadece ses canlıyken; kart yok, pill yok, gölge/glass/gradient yok; Phosphor ikon; sol ray düzeni.
- TAMAM: **waveA** (`8af1a9a` ve sonrası; tokens, Archivo+Martian Mono, Phosphor, UI kit kart/pill'siz, keyboard+RecordKey, wavesurfer v7 waterline player, rail-hung Wave item, /kit; 560 test, 0 axe; peaks 8-bit normalize edildi). Eksik bıraktığı: masaüstü kalıcı oynatıcı/mini şerit (§8.4), profil imza izi (§10), onboarding stroke-draw → C sonrası "A.2 polish" ajanı.
- TAMAM: **fix-audit** (`0cb8132`…`7c47c04`; 6 kırık düzeltildi). TAMAM: **wb-sidecar** (`9cf3373`,`4195bc7`,`fb916ca`; sidecar canlı: arnndn/Matchering/pYIN/peaks; DeepFilterNet Docker'da; 12 CC-BY altyapı parçası seed; migration'lar `20260905100000`, `20260905110000` canlıda).
- ÇALIŞIYOR: **waveB** (Opus; Record/Enhance/Publish ön yüzü, iOS WAV fallback, pitch meter, RNNoise önizleme, trim, /tracks), **waveC** (Opus; tüm ekranlar SCREENS.md'ye göre), **wd-backend** (Sonnet; Open Call, duet_tree RPC, atisma/cypher modları, worker render; ayrıca typecheck düzeltmeleri, format.ts open_call_answered, eslint .venv ignore, sıfır peaks worker bug'ı), **reviewA** (Sonnet; Wave A bağımsız inceleme → `docs/qa/reviewA/REVIEW.md`).
- SONRAKİ: B/C/D-backend bitince → Wave D ön yüz (Duet ekranları: istek, Open Call cevaplama, atışma/cypher kayıt, zincir ağacı, share card) + A.2 polish + reviewA P0 düzeltmeleri; sonra bağımsız tam QA (golden path 390×844, axe, Lighthouse); sonra Wave E (PWA Serwist, perf, TR i18n) ve Wave F (iyzico+Paddle Pro, pitch skoru, harmony); haftalık meydan okuma tablosu/RPC'si de D ile birlikte.
- Sıradaki dalgalar (PRODUCT_V2 §8): B-frontend recorder (waveA sonrası), C ekranlar (waveA sonrası), D Duet v2 (Open Call, zincir, Atışma, Cypher, share card), E PWA/perf/a11y/TR, F Pro abonelik (iyzico+Paddle) + pitch skoru + harmony. Her dalgadan sonra QA ajanı (izole Playwright, 390×844 + 1280×800, axe) ve kod inceleme; ekran görüntüleri `app/docs/qa/<wave>/`.
- Süreçler: dev server ve worker kapalı; ajanlar kendi portlarında açıp kapatıyor.

## (önceki) DURAKLATILDI 2. KEZ (2026-09-05) — "SATILABİLİR KALİTE" YENİDEN İNŞA PROGRAMI, AŞAMA 0
Kullanıcının yeni talimatı: uygulamayı "son raddeye kadar satılabilir kalitede, hatasız" yap; front-end AI görünümlü OLMASIN; güncel (son 1-2 yıl) kaynakları tara; skill/repo/21st.dev kullan; kendine CLAUDE.md hazırla; çok sayıda alt ajan; Fable sadece yönetsin, kod yazmasın.
Program: Aşama 0 araştırma+teşhis → Aşama 1 ürün planı v2 + yeni `app/CLAUDE.md` (tasarım kuralları, yasaklar, mobil kurallar; bunları Fable yazar, kod değil) → Aşama 2 ajan dalgalarıyla yeniden inşa (tasarım sistemi v2, ekran ekran mobil-öncelikli yeniden tasarım, kayıtta "30 sn'de wow": DeepFilterNet temizlik + pitch snap + Matchering; Atışma/Cypher düet modları; haftalık meydan okumalar; VIP abonelik iyzico+Paddle; PWA Serwist; wavesurfer v7; extendable-media-recorder) → Aşama 3 her dalgada bağımsız QA (izole Playwright, mobil emülasyon, axe, Lighthouse) hatasız olana kadar.

Aşama 0 durumu:
- TAMAM: `app/docs/research/2026-09-market-research.md` (5 rapor özeti), `mobile-guidelines.md` (60 kural), `libraries.md` (kütüphane kararları + package delta + requirements.txt önerisi).
- TAMAM: analytics bug düzeltildi (`b4dd0dc`, migration `20260905090000_...` canlıda uygulandı).
- ÇALIŞIYORDU (durdurulduğunda): r-design (Opus) → `app/docs/design/{DESIGN_DNA.json,DESIGN.md,SCREENS.md,REFERENCES.md,refs/}`; r-teardown → `app/docs/research/teardown/`; ux-audit2 → `app/docs/research/ux-audit/REPORT.md` (izole Playwright script ile); debt-pagination TAMAM (`b1c77f5`, migration canlıda). Devam edince: `git status` bak; bu dosyalar varsa aynı brief'le "devral ve bitir" ajanı başlat; eksik dokümanları üreten ajanları yeniden başlat.
- SONRAKİ: tüm Aşama 0 çıktıları gelince Fable `app/docs/PRODUCT_V2.md` + yeni `app/CLAUDE.md` yazar, sonra Aşama 2 dalgalarını başlatır.
- Not: Playwright MCP tarayıcısı ajanlar arasında paylaşılıyor → tarayıcı gereken ajanlara izole `chromium.launch()` script'i kullandır.

## (önceki) DURAKLATILDI (2026-09-04, kullanıcı "dur" dedi) — İYİLEŞTİRME TARTIŞMASI AŞAMASI
Kullanıcı siteyi "vasat ve çalışmıyor" buldu. Kod yazılmayacak; önce tartışma. Görev: "ne çok sattırır, insanların hoşuna ne gider, ne yapabiliriz" — internet araştırmasına dayalı, sadece kanıtlı bulgular.
- 5 araştırma raporu TAMAMLANDI (rakipler, para kazanma, sevilen özellikler, Türkiye pazarı + ilk 10k kullanıcı, yapay zekâ ses özellikleri). Tam metinler transcript'te; özet ana başlıklar:
  - Hayatta kalanlar: asenkron > canlı; işlem/hediye/araç satışı > reklam; keşif motoru şart. Ölüm sebepleri: büyüme penceresinde para kazanmama, canlı-only bağımlılık.
  - Para: 1) ücretli vokal efekti/autotune aboneliği (Voloco modeli, $3-5/ay) ilk test; 2) sanal hediye (Türkiye Bigo'nun 2. pazarı; Yalla/WeSing kanıtı) ikinci adım; karaoke katalog lisansı tuzak; reklam küçük ölçekte değersiz.
  - Özellikler: ilk kayıtta otomatik reverb/compression ("aha" anı), Duet'i merkeze al, backing track/beat kütüphanesi (boş sayfa sorunu), fork/duet zinciri, haftalık tema/open verse, affedici streak, yapılandırılmış 2'ye 1 geri bildirim. Nefret edilenler: sahte etkileşim, taciz, agresif paywall, gecikme.
  - Türkiye: Türkçe-first singing app boşluğu var; Atışma Modu (aşık atışması), Cypher modu, TR tür taksonomisi (arabesk/türkü/rap); 90 gün ~₺220-300k GTM; 5651/KVKK/MESAM yükümlülükleri.
  - AI: pitch snap (autotune), DeepFilterNet temizlik, Matchering mastering, CREPE pitch skoru, self-harmony — hepsi açık kaynak, CPU, ~$0.001/dk; ses klonlama/Suno API yapma.
- UX denetimi ajanı (Playwright ile canlı site) ağ hatasıyla (ENOTFOUND) yarıda kesildi; bulduğu tek somut şey: **/analytics sayfası yüklenmiyor (gerçek bug)**. Ekran görüntüleri scratchpad/ux/ altında olabilir.
- SONRAKİ: kullanıcı "devam" deyince (1) UX denetimini yeniden başlat (aynı brief, Sonnet), (2) 5 raporu + UX bulgularını tek Türkçe sentez halinde sun: sorun teşhisi, 3 aylık öncelik listesi, ilk para kazanma testi, Türkiye lansmanı; (3) kararları alıp ancak ondan sonra kod.

## Sıradaki adımlar (önceki liste)
1. Kullanıcı uygulamayı elle denesin (http://localhost:3333/signup); bulduğu hataları ajanlara düzelttir.
2. Yayın: Vercel'e deploy (docs/DEPLOYMENT.md), worker'ı Docker ile bir sunucuya (Dockerfile.worker), Supabase Auth'ta Site URL + redirect URL ayarı, anahtar rotasyonu.
3. Teknik borç: tek kolonlu cursor kalan 5 dosya, knip bulguları, sesli mesaj peaks'i, byte-doğru yükleme ilerlemesi, PWA ikonları.
4. ~~**Stage 15 (uçtan uca test) — Supabase anahtarı gerekir.** Kullanıcıdan `app/.env.local` doldurmasını iste. Sonra: migration'ları uygula (`supabase db push` veya SQL editor, docs/DEPLOYMENT.md), bucket'ları oluştur, `E2E_SUPABASE=1 npm run e2e` (auth + duet spec'leri), worker'ı ffmpeg ile çalıştırıp gerçek yükleme→işleme→oynatma akışını doğrula. Migration'lar hiç gerçek Postgres'te çalıştırılmadı; hata çıkması normal, düzelttir.
3. İsteğe bağlı iyileştirmeler: audio mesajları için peaks üretimi, byte-doğru yükleme ilerlemesi, public sayfalar için cache'lenebilir kabuk + client hydration, `@next/bundle-analyzer`, PWA ikonları (192/512 PNG).

## Bilinen açık noktalar
- Migration'lar gerçek Postgres'te çalıştırılmadı (Docker erişilemez). Anahtar gelince `supabase db push` ile doğrulanacak.
- Yükleme ilerleme çubuğu aşamalı (byte-doğru değil).
- Sesli mesajlar dekoratif waveform kullanıyor (peaks üretilmiyor).
- Wave detayında "sohbete gönder" UI'ı etkileşim ajanının paylaşım penceresine bağlı.
